#' Example Binary Classifiers Dataset
#'
#' A synthetic dataset with two classifiers (A and B) and a naive baseline,
#' designed to illustrate the distinction between agreement and skill metrics
#' and the effect of the tipping-point position on AFOM vs. AUC.
#'
#' @format A named list with elements:
#' \describe{
#'   \item{observed}{Logical vector of length 500. True class labels (prevalence ~ 0.20).}
#'   \item{scores_A}{Numeric vector. Classifier A scores (AUC ~ 0.82, tipping point near P).}
#'   \item{scores_B}{Numeric vector. Classifier B scores (similar AUC, different tipping point).}
#'   \item{scores_naive}{Numeric vector. Constant-score naive model (AUC = 0.5).}
#'   \item{N}{Integer. Total number of cases (500).}
#'   \item{prevalence}{Numeric. Empirical prevalence.}
#' }
#' @source Generated by `data-raw/create_example_data.R`.
#' @examples
#' data(example_classifiers)
#' clf_a <- toc_from_scores(example_classifiers$scores_A,
#'                          example_classifiers$observed)
#' plot_fom(clf_a, classifier_name = "Classifier A")
"example_classifiers"
